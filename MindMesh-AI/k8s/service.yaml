apiVersion: v1
kind: Service
metadata:
  name: mindmesh-service
  namespace: mindmesh
  labels:
    app: mindmesh-ai
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: mindmesh-ai
